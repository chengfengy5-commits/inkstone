# 任务：稳定化中文学习工作台

## Delivery / Proof Map

| 交付能力 | 主要影响范围 | 可观察结果 | 证明方式 |
| --- | --- | --- | --- |
| 跨来源稳定去重 | `scripts/learning-library-collector.mjs` | 同一仓库或文章只保留一个候选，60 天内不重复 | Node 固定夹具测试 |
| 独立中文学习笔记 | 采集器渲染与验证 | 每日默认 8 篇独立 Markdown，元数据、中文标签、原始链接和章节齐全 | 渲染快照/结构测试 |
| 每日导航索引 | 采集器索引写入 | 1 篇索引链接全部独立笔记，不复制完整正文 | 索引结构和失败边界测试 |
| 幂等续跑 | pending、报告缓存、MCP operation ID | 中途失败后重跑不重复创建笔记 | MCP 假实现故障注入测试 |
| 可审计运行记录 | `StateDirectory` 运行状态 | 能直接判断阶段、计数、警告、错误、尝试次数和结果 | 状态转换与脱敏测试 |
| 单一生产定时器 | `deploy/systemd/`、运行手册和切换工具 | 新 timer 启用，旧 timer 停用，历史数据保留且可回滚 | 静态测试与 VPS 状态证据 |

## 1. 锁定已有行为并建立测试边界

- [x] 1.1 梳理 `cdbd47e` 已完成的候选采集、源码证据、8 篇独立笔记、索引、pending、seen 和 systemd 行为，形成不重复实现清单；受影响范围：采集器测试与本变更文档；结果：后续任务只补缺口；证据：`git diff cdbd47e --stat` 与需求映射检查。
- [x] 1.2 将选择验证、标签生成、独立笔记渲染、索引渲染和状态辅助逻辑暴露为可测试的纯函数，不改变命令行入口；受影响范围：`scripts/learning-library-collector.mjs`；结果：脚本仍可直接运行且测试可导入核心行为；证据：`node --test scripts/learning-library-collector.test.mjs`。
- [x] 1.3 增加固定候选和报告夹具，证明默认 4/2/2、至少两个 AI、至少两个后端/JVM、完整 Front Matter、中文标签、原始链接、必需学习章节与真实代码路径过滤；受影响范围：采集器测试；结果：用户可见输出被回归测试锁定；证据：Node 测试通过。

## 2. 实现跨来源内容去重

- [x] 2.1 实现普通 URL 规范化和 GitHub 仓库内容键，保留来源 ID 用于审计；受影响范围：候选建模与辅助函数；结果：跟踪参数、片段、尾斜杠和 GitHub 多入口不再产生不同学习主体；依赖：任务 1；证据：URL 与仓库参数化测试。
- [x] 2.2 在候选准备、模型选择验证、seen 持久化和 60 天裁剪中统一使用内容键，并兼容读取已有来源 ID 状态；受影响范围：pending/seen 状态；结果：升级不丢失已有去重保护，同批次及近期跨源重复被拒绝；依赖：2.1；证据：旧状态迁移、同批次和跨日期测试。
- [x] 2.3 记录去重前后和按来源分类的候选计数；受影响范围：运行摘要；结果：可判断候选减少是正常去重还是来源故障；依赖：2.2；证据：状态记录断言。

## 3. 增加可审计运行状态和安全失败

- [x] 3.1 实现原子运行记录及阶段转换，记录日期、尝试次数、阶段、时间、候选/入选/写入计数、来源警告、结果和清理后的错误；受影响范围：`StateDirectory` 运行文件；结果：不解析日志即可定位进度和失败；依赖：任务 1；证据：状态机单元测试。
- [x] 3.2 在采集、选择、证据补充、报告生成、笔记写入、索引写入和成功收尾边界更新运行记录；受影响范围：采集器主流程；结果：所有退出路径均留下最后阶段；依赖：3.1；证据：阶段故障注入测试。
- [x] 3.3 对错误文本进行脱敏并限制状态文件字段，禁止写入令牌、Authorization 头、凭据正文和候选长正文；受影响范围：错误序列化；结果：运行诊断不泄露秘密；依赖：3.1；证据：敏感字符串测试。
- [x] 3.4 使用 MCP 假客户端模拟部分笔记成功、索引失败和重跑，证明 pending 与稳定 operation ID 可续跑且不产生重复笔记；受影响范围：主流程测试接缝；结果：幂等行为有直接证据；依赖：1.2、3.2；证据：故障注入测试。

## 4. 收敛 systemd 定时任务并完善运维文档

- [ ] 4.1 增加幂等、无数据删除的统一采集器安装/切换工具或等价受测命令清单，启用 `inkstone-tech-digest.timer` 并停用 `inkstone-github-trending.timer`、`inkstone-ai-frontier.timer`；受影响范围：`deploy/systemd/` 与部署脚本；结果：服务器只保留一个新内容入口；依赖：任务 2、3；证据：静态测试或隔离 systemd 环境验证。
- [ ] 4.2 为切换工具增加预检、结果检查和只改变 timer enablement 的回滚模式，不删除历史笔记、状态目录、凭据或旧 unit；受影响范围：部署切换；结果：重复执行安全且可逆；依赖：4.1；证据：命令生成/脚本测试与 `shellcheck`（若环境提供）。
- [ ] 4.3 更新中文运行手册，覆盖计划时间、手动触发、运行记录、journal、失败重试、候选不足、凭据问题、切换和回滚；受影响范围：`docs/tech-learning-digest.md`；结果：用户能独立判断任务是否成功及如何恢复；依赖：4.1、4.2；证据：文档命令与实际 unit 名称核对。

## 5. 完成本地验证、审查和部署准备

- [ ] 5.1 运行采集器 Node 测试、项目单元测试、VPS 测试、类型检查、VPS 构建、注释/i18n 检查、`git diff --check`；结果：无回归且生产构建可用；依赖：任务 1–4；证据：完整命令输出。
- [ ] 5.2 对本批次执行 Grill Me 规格质询和代码质量审查，逐条挑战重复识别、错误脱敏、失败恢复、旧任务切换、测试有效性和非目标功能未被引入；结果：所有有效质疑均有测试、修复或明确证据，复审通过；依赖：5.1；证据：`grill-me-review.md`。
- [ ] 5.3 提交并推送精确 Git 版本，生成 VPS 切换前检查单，但不在未单独确认时修改生产服务器；结果：代码与操作步骤可部署且来源可追溯；依赖：5.2；证据：提交 SHA、远端分支和干净工作区。
- [ ] 5.4 在获得生产部署授权后，备份相关 unit 与状态目录，部署精确提交，执行切换并手动运行一次，验证 8 篇独立笔记、1 篇索引、中文目录/标签、原始链接、运行记录、旧 timer 停用和次日计划；结果：线上第一批验收完成；依赖：5.3；证据：systemd、journal、Inkstone 页面/API 与 Git 版本记录。
