# Grill Me Review

## 第一批质询结论

1. 普通笔记只有在标签以 `学习状态/` 开头时才会被纳入；正文里出现状态词或普通标签不会误入。
2. 同一笔记只分类一次。多个不同状态进入唯一的“需整理”泳道，重复的同名状态会去重后进入正常泳道。
3. 未知状态不会隐藏，也不会猜测映射；统一进入“需整理”并展示原始状态，方便人工修正。
4. 分类器先排除 `isArchived` 或 `deletedAt` 非空的摘要，不会污染计数。
5. 工作台只消费现有 `NoteSummary[]`，没有调用正文读取接口；卡片打开时才沿用原有 `openNote` 流程加载目标笔记。
6. 卡片先关闭 Panel，再调用既有 notes store 的 `openNote(id)`。组件测试和手机端浏览器验收均证明能打开对应笔记。
7. 手机窄屏实测采用单列纵向泳道，标题、计数和关闭按钮无重叠，没有横向溢出；所有入口与卡片使用现有可点击控件尺寸。桌面布局由响应式断点切换为双列/五列，组件结构不复制移动端逻辑。
8. 第一批仅增加纯分类器、客户端面板、入口和文案；没有数据库迁移、后台提醒或采集改动。

## 第一批验证证据

- 纯分类器与面板行为测试：6/6 通过。
- 手机端从“我的 → 学习工作台”进入，空状态与一条“待实践”笔记状态均验证通过。
- 点击“并发控制实践”卡片后面板关闭，并打开同名笔记。
- 全量单元测试 117/117、VPS 测试 40/40 通过。
- 类型检查、i18n、注释策略、VPS 发布构建和 `git diff --check` 通过。

## 第二批质询结论

1. 状态切换使用原生下拉框，支持键盘和触屏；异常笔记必须明确选择目标状态，不做自动猜测。
2. 未打开的笔记通过 `openNote(id, { navigate: false })` 复用缓存恢复、离线日志和服务端读取逻辑，不改变当前工作区或深链接。
3. 写入仍调用 store 的 `editContent`，继续走现有本地持久化、自动保存、冲突重放和失败回滚链路，没有旁路请求。
4. 状态重写只处理 `学习状态/*` 标签：已有目标状态时删除冲突状态，没有状态时追加一个正文标签；其他 Front Matter、正文、链接和标签保持不变。
5. 后台读取失败时不写入半成品，并显示错误反馈；切换期间控件禁用，避免同一笔记重复提交。

## 第二批验证证据

- 状态分类、Markdown 重写和面板行为定向测试 12/12 通过。
- 桌面端实测把“JVM 并发学习”从“待实践”切换到“学习中”，卡片立即迁移泳道。
- 实测正文中的 `[[原始链接]]`、说明文字和 `主题/Java` 标签保持不变，只有学习状态变为 `学习状态/学习中`。
- 1180px 面板五列完整显示，标题、计数、下拉框和关闭按钮没有重叠或横向溢出。
- 全量单元测试 123/123、VPS 测试 40/40、类型检查、i18n、注释策略、VPS 发布构建和 `git diff --check` 通过。

## 第三批质询结论

1. 日期采用本地日历的 `YYYY-MM-DD`，不通过 UTC 字符串截断；不存在的日期、无效字段和损坏的 Front Matter 不会被静默改写。
2. `复习日期` 和 `上次学习` 只修改顶层同名标量；原有换行风格、其他 Front Matter、正文、链接和标签保持不变。
3. 复习队列仅后台加载“待复习”笔记正文，不读取其他泳道正文，也不改变当前笔记或深链接。
4. 队列明确区分已逾期、今天到期、未来计划、未安排和日期无效；逾期及未来计划先按日期升序，再按更新时间、标题和 ID 确定性排序。
5. 状态迁移会记录当天 `上次学习`；复习日期由用户通过原生日期控件明确选择，并继续走现有离线写入链路。

## 第三批验证证据

- 学习功能与编辑器回归定向测试 19/19 通过，覆盖日期边界、非法日期、损坏 Front Matter、CRLF 保留、五类队列、日期写入和重叠 Markdown 装饰排序。
- 桌面实测：`2026-09-22` 显示“今天到期”，修改为 `2026-09-30` 后立即进入“未来计划”。
- 手机窄屏实测：复习队列、五个泳道、状态下拉和日期控件单列显示，无横向溢出、遮挡或重叠。
- 浏览器验收暴露的 CodeMirror 装饰排序异常已修复：任务、标签和 Wiki 链接装饰先去重，再由 CodeMirror 按范围与边界规则统一排序，状态更新不再触发编辑器插件崩溃。
- 最终全量单元测试 130/130、VPS 测试 40/40、类型检查、i18n、注释策略、VPS 发布构建和 `git diff --check` 通过。
